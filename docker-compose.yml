version: '3'

services:
  postgres:
    image: postgres:latest
    environment:
      - 'POSTGRES_DB=test'
      - 'POSTGRES_USER=test'
      - 'POSTGRES_PASSWORD=test123'
    ports: 
      - '5433:5432'